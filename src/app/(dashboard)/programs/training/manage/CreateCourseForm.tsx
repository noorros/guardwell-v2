// src/app/(dashboard)/programs/training/manage/CreateCourseForm.tsx
//
// Phase 4 PR 4 — placeholder. Full implementation lands in step 6 of
// this PR's commit sequence (next commit). Kept as a typed stub so
// ManageCoursesTab.tsx + its test compile during step 5.

"use client";

export interface CreateCourseFormProps {
  onSuccess: () => void;
}

export function CreateCourseForm({ onSuccess: _onSuccess }: CreateCourseFormProps) {
  return (
    <div className="text-sm text-muted-foreground">
      Form implementation lands in the next commit.
    </div>
  );
}
